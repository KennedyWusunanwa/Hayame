# Daily Log - 2025-12-27

## Summary
- Improved mobile navigation and accessibility, cleaned up hero search UX on mobile, and refined the Explore page layout for responsiveness.
- Added Paystack-backed booking with host approval/refunds, expanded car features/icons, and ensured edit pages load defaults. Mobile-friendly dashboard tables and quick-nav added.
- Signup/profile now collect first/last name, city, optional avatar; initials render when no photo. Supabase image allowlist and Paystack env scaffolding added.

## Changes
- **Navbar mobile sheet**
  - Ensured the Radix sheet uses a visible white panel with dark text and a description for accessibility.
  - Added explicit open/close handling and transition classes so the menu panel appears over the dark overlay.
  - Included all header actions in the mobile sheet (Log in/Sign up when logged out; user badge, List your car, and Sign out when logged in).
- **Hero search (homepage)**
  - Date input now shows a "Dates" placeholder on mobile; switches to native date picker on focus so the placeholder is visible when unfocused.
- **Explore page responsiveness**
  - Adjusted container padding and grid to avoid clipping on small screens.
  - Map panel gets constrained height with overflow handling on mobile; card grid now breaks 1/2/3 cols (mobile/tablet/desktop).
  - Sidebar column widened on desktop; markers cleaned up to `city → name` with price formatting.
- **Bookings + payments**
  - Booking flow now requires Paystack payment (`/api/bookings/paystack`) and moves to `awaiting_host`; hosts can approve or reject (reject triggers refund) via `/api/bookings/[id]`.
  - Dashboard bookings split into “Your bookings” and “Bookings on your cars” with mobile cards; actions available only for hosts when awaiting approval.
  - Added booking status/payment fields to types/migration; blocked direct POST `/api/bookings`.
- **Sign up & profiles**
  - Signup captures first/last name, city, optional avatar upload; upserts profile on signup/payment flows to satisfy FK.
  - Profile page now has first/last name fields, city, avatar upload; initials shown when no avatar (also used in host cards/sidebar).
  - Next image allowlist includes Supabase host; Paystack keys added to env example; mobile dashboard nav added.
- **Cars/features**
  - Expanded feature list/icons for car details; edit defaults load correctly. Mobile cards for inventory list.

## Configuration / Environments
- **Repository:** `main` branch on `https://github.com/KennedyWusunanwa/Hayame.git`.
- **Local dev:** `npm run dev` (Next.js 16, React 19). Use `.env.local` with Supabase URL/anon key as in `.env.example`.
- **Vercel:** Builds from `main`; set `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `PAYSTACK_SECRET_KEY`, `NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY`, `NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET`/avatar bucket in the project.
- **Database:** Supabase (URL/anon key from `.env.example`); migrations and RLS are in `db/migration.sql` and `docs/RLS.md`.

## Outstanding / Follow-ups
- Verify Vercel preview after these pushes to confirm mobile menu visibility, Explore layout, and Paystack popup on real devices.
- Run Supabase SQL updates for new booking statuses/payment fields and profile first/last columns; ensure storage bucket is public for avatars.
